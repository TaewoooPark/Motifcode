# corpus

Vendored, verifiable inputs. Nothing here is generated by hand.

| file | origin |
|---|---|
| `chat_template.jinja` | `Motif-Technologies/Motif-3` on Hugging Face, verbatim. MIT. |
| `template-golden/cases.json` | Produced by `toolkit/fixtures/gen_template_golden.py` by rendering the file above. Regenerate, never edit. |

The tool-call repair corpus lives in `packages/protocol/test/toolcall.test.ts`
rather than here, because each case carries a comment explaining the failure it
represents. It is ported from `tests/tool_parsers/test_motif_tool_parser.py` in
the Motif vLLM fork.
