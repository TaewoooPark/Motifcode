# What CI installs to run the Python toolkit tests.
#
# CPU torch on purpose: the tests exercise tensor slicing, index arithmetic and
# safetensors round-trips, none of which need a GPU. What they do need is a real
# tensor backend — with torch absent the slicing test skipped, so the one path
# that decides whether a pruned checkpoint is correct was never executed by CI,
# on any commit, ever.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.11.0
safetensors==0.8.0
numpy==2.3.4
jinja2==3.1.6
