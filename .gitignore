node_modules/
dist/
*.tsbuildinfo
.venv/
__pycache__/
*.pyc
.DS_Store
.motif/
*.log

# Large model artifacts never belong in the repo.
*.safetensors
*.gguf
# goldens are committed on purpose: CI must run without Python

# Pruning work products: profiles, keep-lists and checkpoints. The normative
# guide itself is tracked at docs/model_guide.md — code and guide drift only
# gets caught in review if both are in the same commit.
prune-work/

# Patch handoff and working notes — process documents, not deliverables.
PATCH_HANDOFF.md
HANDOFF*.md
NOTES*.md
WORKLOG*.md
scratch/
